#ifndef EVENTUNIT_H
#define EVENTUNIT_H

#include <iostream>
#include <vector>
#include <string>

class EventUnit {
private:

public:
    EventUnit();
    virtual ~EventUnit();
};

#endif // EVENTUNIT_H
